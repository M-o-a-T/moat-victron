# The VE USB RS485 has a FT232R, but that chip can also rs232 etc, so also check default
ACTION=="add", ENV{ID_BUS}=="usb", ENV{ID_VENDOR}=="Micropython", ENV{VE_SERVICE}="ignore"
ACTION=="add", ENV{ID_BUS}=="usb", ENV{ID_MODEL}=="CP2104_USB_to_UART_Bridge_Controller",  ENV{VE_SERVICE}="lmodbus"
